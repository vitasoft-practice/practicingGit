import { RemoteVersion } from '../version';
import { VersionList } from './version-list';
export declare class RemoteVersionList extends VersionList {
    entries: RemoteVersion[];
    constructor(entries: RemoteVersion[]);
    sortBySemver(order?: 'asc' | 'desc'): RemoteVersionList;
}

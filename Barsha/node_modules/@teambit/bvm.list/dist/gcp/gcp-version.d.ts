import { RemoteVersion } from '../version';
export declare class GcpVersion {
    private fileName;
    private host;
    private md5hash;
    private protocol;
    constructor(fileName: string, host: string, md5hash: string, protocol?: string);
    get version(): string;
    get url(): string;
    toRemoteVersion(): RemoteVersion;
    toObject(): {
        url: string;
        version: string;
    };
}

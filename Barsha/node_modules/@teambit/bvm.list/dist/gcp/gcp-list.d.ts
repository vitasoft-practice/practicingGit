import { GcpStorage } from '@teambit/gcp.storage';
import { RemoteVersionList } from '../version-list';
export declare class GcpList {
    private gcpStorage;
    private osType;
    private releaseType;
    constructor(gcpStorage: GcpStorage, osType?: string, releaseType?: string);
    list(): Promise<RemoteVersionList>;
    rawFiles(): Promise<any>;
    static create(releaseType?: string, osType?: string, proxyConfig?: {}): GcpList;
}
